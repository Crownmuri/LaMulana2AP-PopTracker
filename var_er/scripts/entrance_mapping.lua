ER_ENTRANCE_NAMES = {
    -- Frontside
    ["er_starting_area"] = "Starting Area",
    ["er_village_of_departure_next_to_xelpud"] = "Village Next to Xelpud (D-4)",
    ["er_village_of_departure_main__f_5"] = "Village Ruins Entrance (F-5)",
    ["er_village_of_departure_ladder_down__f_3"] = "Village Ladder Down (F-3)",
    ["er_gate_of_guidance_main_entrance__c_1"] = "Guidance Main Entrance (C-1)",
    ["er_gate_of_guidance_ladder_down__a_6"] = "Guidance Ladder Down (A-6)",
    ["er_gate_of_guidance_left_gate__a_3"] = "Guidance Left Gate (A-3)",
    ["er_inferno_cavern__b_1"] = "Inferno Cavern (B-1)",
    ["er_mausoleum_of_giants_left_door__a_5"] = "Mausoleum Bottom Left (A-5)",
    ["er_mausoleum_of_giants_ladder_up__a_1"] = "Mausoleum Ladder Up (A-1)",
    ["er_endless_corridor__c_1"] = "Endless Corridor (C-1)",
    ["er_gate_of_illusion_left_gate__a_1"] = "Gate of Illusion Left (A-1)",
    ["er_gate_of_illusion_right_gate__c_1"] = "Gate of Illusion Right (C-1)",
    ["er_roots_of_yggdrasil_main_gate__d_4"] = "Yggdrasil Main Gate (D-4)",
    ["er_roots_of_yggdrasil_top_left_switch_gate__a_1"] = "Yggdrasil Top Left Gate (A-1)",
    ["er_roots_of_yggdrasil_top_middle_nidhogg_gate__d_1"] = "Yggdrasil Top Mid Gate (D-1)",
    ["er_roots_of_yggdrasil_top_right_birth_gate__g_1"] = "Yggdrasil Top Right Gate (G-1)",
    ["er_roots_of_yggdrasil_ladder_down__c_5"] = "Yggdrasil Ladder Down (C-5)",
    ["er_annwfn_ladder_up__c_1"] = "Annwfn Ladder Up (C-1)",
    ["er_annwfn_bottom_one_way_ladder__e_5"] = "Annwfn One-Way Ladder (E-5)",
    ["er_annwfn_right_gate__g_4"] = "Annwfn Right Gate (G-4)",
    ["er_annwfn_bifrost"] = "Annwfn Bifrost",
    ["er_immortal_battlefield_left_gate__a_6"] = "IB Left Side Gate (A-6)",
    ["er_immortal_battlefield_bifrost_fall"] = "IB Bifrost Fall",
    ["er_immortal_battlefield_cetus_up_ladder__f_1"] = "IB Cetus Ladder Up (F-1)",
    ["er_immortal_battlefield_right_door__h_4"] = "IB Right (H-4)",
    ["er_immortal_battlefield_down_ladder_near_spinning_wheel__d_7"] = "IB Wheel Ladder Down (D-7)",
    ["er_immortal_battlefield_alviss_down_ladder__g_7"] = "IB Alviss Ladder Down (G-7)",
    ["er_immortal_battlefield_moon_altar_hallway__g_7"] = "IB Moon Altar Hallway (G-7)",
    ["er_immortal_battlefield_left_altar_door__d_3"] = "IB Dinosaur Left (D-3)",
    ["er_immortal_battlefield_right_altar_door__f_3"] = "IB Dinosaur Right (F-3)",
    ["er_icefire_treetop_middle_gate__d_3"] = "Icefire Middle Gate (D-3)",
    ["er_icefire_treetop_fire_side_up_ladder__c_1"] = "Icefire Fire Ladder Up (C-1)",
    ["er_icefire_treetop_ice_side_left_ladder__f_1"] = "Icefire Ice Left Ladder (F-1)",
    ["er_icefire_treetop_ice_side_right_ladder__f_1"] = "Icefire Ice Right Ladder (F-1)",
    ["er_divine_fortress_left_gate__a_3"] = "DF Gate near Twins (A-3)",
    ["er_shrine_of_the_frost_giants_bergelmir_gate__b_4"] = "SFG Bergelmir Gate (B-4)",
    ["er_shrine_of_the_frost_giants_backside_gate__b_2"] = "SFG Backside Gate (B-2)",
    ["er_gate_of_the_dead_wedjat_gate__f_5"] = "GotD 4 Switches Gate (F-5)",
    ["er_takamagahara_shrine_bottom_gate__c_7"] = "Takama Bottom Gate (C-7)",
    ["er_takamagahara_shrine_neck"] = "Takama Dragon's Neck",
    ["er_heavens_labyrinth_gate__d_1"] = "HL 4 Pillars Gate (D-1)",
    ["er_heavens_labyrinth_monster_s_jaw"] = "HL Monster's Jaw",
    -- Backside
    ["er_valhalla_gate__a_2"] = "Valhalla Main Gate (A-2)",
    ["er_dark_star_lord_s_mausoleum_gate__d_7"] = "DSLM Main Gate (D-7)",
    ["er_dark_star_lord_s_mausoleum_pyramid"] = "DSLM Pyramid to Nibiru",
    ["er_ancient_chaos_gate__d_6"] = "AC Main Gate (D-6)",
    ["er_hall_of_malice_gate__c_1"] = "HoM Top Gate (C-1)",
    ["er_nibiru_spaceship"] = "Nibiru (B-3)",
    ["er_altar_left_door__a_1"] = "Altar Left Gate (A-1)",
    ["er_altar_right_door__c_1"] = "Altar Right Gate (C-1)",
    ["er_cavern_left_door__a_1"] = "Cavern Left (A-1)",
    ["er_cavern_right_door__d_1"] = "Cavern Right (D-1)",
    ["er_cliff__a_1"] = "Cliff (A-1)",
    -- Soul Gates
    ["er_roots_of_yggdrasil_bottom_soul_gate__d_6"] = "Ygg Bottom SG (D-6)",
    ["er_annwfn_soul_gate__a_4"] = "Ann Bottom Left SG (A-4)",
    ["er_immortal_battlefield_top_right_gate__h_2"] = "IB Top Right SG (H-2)",
    ["er_immortal_battlefield_bottom_left_gate__b_7"] = "IB Bottom Left SG (B-7)",
    ["er_immortal_battlefield_spiral_boat_soul_gate_d_4"] = "IB Spiral Boat SG (D-4)",
    ["er_icefire_treetop_under_ratatoskr_soul_gate__g_3"] = "IF Ratatoskr SG (G-3)",
    ["er_icefire_treetop_vidofnir_soul_gate__d_6"] = "IF Vidofnir SG (D-6)",
    ["er_divine_fortress_soul_gate__c_5"] = "DF Bottom Left SG (C-5)",
    ["er_shrine_of_the_frost_giants_balor_soul_gate__e_1"] = "SFG Balor SG (E-1)",
    ["er_shrine_of_the_frost_giants_main_soul_gate__e_4"] = "SFG Main SG (E-4)",
    ["er_gate_of_the_dead_soul_gate__c_4"] = "GotD Main SG (C-4)",
    ["er_takamagahara_shrine_top_main_soul_gate__d_1"] = "Takama Top Main SG (D-1)",
    ["er_takamagahara_shrine_belial_soul_gate__b_1"] = "Takama Belial SG (B-1)",
    ["er_heavens_labyrinth_soul_gate__e_5"] = "HL Main SG (E-5)",
    ["er_valhalla_soul_gate__e_2"] = "Val Top Right SG (E-2)",
    ["er_ancient_chaos_soul_gate__c_1"] = "AC Blood SG (C-1)",
    ["er_hall_of_malice_soul_gate__d_3"] = "HoM Hidden SG (D-3)",
    ["er_eternal_prison_gloom_soul_gate__d_2"] = "EP Gloom SG (D-2)",
}

-- Global pairing table: ER_PAIRINGS[codeA] = codeB (bidirectional)
-- This is read by logic.lua for flood-fill routing
ER_PAIRINGS = {}

ENTRANCE_SELECTED = nil

-- Frames during which the click watcher should ignore state changes.
-- AddWatchForCode fires for ANY provided-code change — including the programmatic
-- Active toggles that happen during initial state restore, Import, or our own
-- ApplyPairing. Without this guard, a restored Active=true triggers EntranceClick,
-- which sees the (also-restored) pairing and treats it as an unpair click.
ER_SUPPRESS_FRAMES = 0

function ClearTargetOverlay(code)
    local target = Tracker:FindObjectForCode("target_" .. code)
    if target then
        target:SetOverlay("")
        target:SetOverlayBackground("#00000000")
    end
end

function SetTargetOverlay(code, destination_code)
    local target = Tracker:FindObjectForCode("target_" .. code)
    if target then
        target:SetOverlayAlign("left")
        target:SetOverlayFontSize(10)
        target:SetOverlay(ER_ENTRANCE_NAMES[destination_code] or destination_code)
        target:SetOverlayBackground("#FF222222")
    end
end

function UnlinkEntrance(code)
    local paired = ER_PAIRINGS[code]
    if not paired then return end

    -- Clear both sides
    ClearTargetOverlay(code)
    ClearTargetOverlay(paired)

    -- Reset toggle states on both entrances
    local objA = Tracker:FindObjectForCode(code)
    local objB = Tracker:FindObjectForCode(paired)
    if objA then objA.Active = false end
    if objB then objB.Active = false end

    ER_PAIRINGS[code] = nil
    ER_PAIRINGS[paired] = nil

    print("Unlinked: " .. (ER_ENTRANCE_NAMES[code] or code) .. " <-> " .. (ER_ENTRANCE_NAMES[paired] or paired))
end

function EntranceClick(code)
    if ER_SUPPRESS_FRAMES > 0 then return end

    local obj = Tracker:FindObjectForCode(code)
    if not obj then
        print("EntranceClick: could not find object for " .. code)
        return
    end

    -- 1. If clicking the same one again, deselect
    if ENTRANCE_SELECTED == code then
        ENTRANCE_SELECTED = nil
        print("Selection Canceled")
        return
    end

    -- 2. If nothing is selected
    if ENTRANCE_SELECTED == nil then
        -- If already linked, unlink it
        if ER_PAIRINGS[code] then
            UnlinkEntrance(code)
            return
        end
        -- Otherwise select for pairing
        ENTRANCE_SELECTED = code
        print("Source Selected: " .. (ER_ENTRANCE_NAMES[code] or code))
        return
    end

    -- 3. Pair them (Bidirectional A <-> B)
    local codeA = ENTRANCE_SELECTED
    local codeB = code

    -- If either side is already linked, unlink first
    if ER_PAIRINGS[codeA] then UnlinkEntrance(codeA) end
    if ER_PAIRINGS[codeB] then UnlinkEntrance(codeB) end

    -- Store pairing
    ER_PAIRINGS[codeA] = codeB
    ER_PAIRINGS[codeB] = codeA

    -- Update target labels
    SetTargetOverlay(codeA, codeB)
    SetTargetOverlay(codeB, codeA)

    print("Linked: " .. (ER_ENTRANCE_NAMES[codeA] or codeA) .. " <-> " .. (ER_ENTRANCE_NAMES[codeB] or codeB))

    -- Reset selection
    ENTRANCE_SELECTED = nil
end

function InitEntranceWatchers()
    if not ER_ENTRANCE_NAMES then return end
    for code, _ in pairs(ER_ENTRANCE_NAMES) do
        ScriptHost:AddWatchForCode("click_" .. code, code, function(c)
            EntranceClick(c)
        end)
    end
end

InitEntranceWatchers()

-- ============================================================
-- Persistence: save/restore ER_PAIRINGS across save/load cycles.
-- PopTracker only persists standard item state (Active, stages, counts),
-- so without this the custom entrance pairing table is lost on reload.
-- ============================================================

-- Frame budget that absorbs the state-restore phase. State restore (auto-load
-- on launch and manual Import) doesn't expose a "restore complete" callback,
-- so we hold the watcher off for a window of frames and decay one per frame.
local SUPPRESS_RESTORE_FRAMES = 30

local function SuppressClicks(frames)
    if frames > ER_SUPPRESS_FRAMES then ER_SUPPRESS_FRAMES = frames end
end

-- Initial guard for the auto-restore that runs after script load completes.
SuppressClicks(SUPPRESS_RESTORE_FRAMES)

if ScriptHost.AddOnFrameHandler then
    ScriptHost:AddOnFrameHandler("er_suppress_decay", function(_)
        if ER_SUPPRESS_FRAMES > 0 then
            ER_SUPPRESS_FRAMES = ER_SUPPRESS_FRAMES - 1
        end
    end)
end

local function ApplyPairing(a, b)
    ER_PAIRINGS[a] = b
    ER_PAIRINGS[b] = a
    SetTargetOverlay(a, b)
    SetTargetOverlay(b, a)
    -- Restore the toggle state so the entrances show as checked, mirroring
    -- the manual pairing flow (UnlinkEntrance flips these back to false).
    local objA = Tracker:FindObjectForCode(a)
    local objB = Tracker:FindObjectForCode(b)
    if objA then objA.Active = true end
    if objB then objB.Active = true end
end

local function SavePairingsFunc(self)
    -- Store one direction per pair; the load side rebuilds the symmetric mapping.
    local seen = {}
    local pairs_list = {}
    for a, b in pairs(ER_PAIRINGS) do
        if not seen[a] and not seen[b] then
            seen[a] = true
            seen[b] = true
            table.insert(pairs_list, { a, b })
        end
    end
    return { Name = self.Name, Pairings = pairs_list }
end

local function LoadPairingsFunc(self, data)
    -- Re-arm the suppression window: covers our own ApplyPairing calls below
    -- AND any toggle-restore the engine fires on its side of this pass.
    SuppressClicks(SUPPRESS_RESTORE_FRAMES)
    ER_PAIRINGS = {}
    if type(data) ~= "table" or data.Name ~= self.Name then return end
    local pairs_list = data.Pairings
    if type(pairs_list) ~= "table" then return end
    for _, entry in ipairs(pairs_list) do
        local a, b = entry[1], entry[2]
        if type(a) == "string" and type(b) == "string" then
            ApplyPairing(a, b)
        end
    end
end

local pairings_storage = ScriptHost:CreateLuaItem()
pairings_storage.Name = "er_pairings_storage"
pairings_storage.CanProvideCodeFunc = function(self, code) return code == self.Name end
pairings_storage.SaveFunc = SavePairingsFunc
pairings_storage.LoadFunc = LoadPairingsFunc
